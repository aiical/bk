
<style type="text/css">
    .table-bordered td {
        cursor: pointer;
    }

        .table-bordered td:hover {
            background: #d8fbac;
        }

    .today-course li {
        min-height: 60px;
        line-height: 38px;
        background: #fff;
        border: 1px dashed rgb(221, 221, 221);
        font-size:18px;
        cursor: pointer;
        padding:1em 2em;
    }

        .today-course li span:hover {
            height: 100%;
            width: 100%;
            background: #d8fbac;
        }
    .course-arrange-status span {
        display: block;
        float: left;
        margin: 3px 5px 1px 2px;
        text-align: center;
        line-height: 10px;
    }
    .course-status-common {
        width: 100%;
        margin: 1px;
        font-size: 14px;
        padding: 5px 5px;
        color: white;
    }
    .Normal {
        background-color: #8763B3;
    }

    .Effective {  
        background-color: #37A7DB;
    }

    .Cancel {
        background-color: #E6342E;
    }

    .Finished {
        background-color: #56AE65;
    }
</style>
<div class="course-arrange-status">
    <div style="display: inline; float: right; padding-bottom: 2px;" class="colorarea">
        <span style="cursor: pointer;"><span class="course-status-common Normal" style="width: auto; padding-right: 10px;"></span><span class="named">待放课</span></span>
        <span style="cursor: pointer;"><span class="course-status-common Effective" style="width: auto; padding-right: 10px;"></span><span class="named">已放课</span></span>
    </div>
</div>
    <div ng-controller="app.views.home as vm">
        <input type="hidden" value="@DateTime.Now.ToShortDateString()" id="beginTime" />
        <input type="hidden" value="@DateTime.Now.AddDays(1).ToShortDateString()" id="endTime" />
        <div class="row">
            <div class="col-md-12" style="margin-bottom:20px;">
                <h4>{{vm.todaySignInRecord}}</h4>@*截止到目前，今天已经给 张三上课 8：00-9：00，李四上课 11：00-12：00  <br />合计上课两个小时*@
            </div>
            <div class="col-md-12">
                <ul class="today-course" ng-repeat="course in vm.schedule">
                    <li>
                        <span> {{course.beginTime|date:"HH:mm"}}-{{course.endTime|date:"HH:mm"}}</span>
                        <span class="course-status-common {{course.courseStatus}}  remark">{{course.remark}} </span>
                    </li>
                </ul>
                @*<table class="table table-bordered">

                        <tbody>
                            @foreach (var hour in classHours)
                            {
                                <tr>

                                    <td id="td_1" class=" calendarItem" count="0" date="2017-05-28" style="border: 1px solid rgb(221, 221, 221);">
                                        @hour
                                        <div class="records">
                                        </div>
                                    </td>
                                </tr>
                            }
                            <tr></tr>

                        </tbody>
                    </table>*@
            </div>
        </div>
    </div>
